diff -uNr libzypp-bindings-0.5.9/swig/Capabilities.i libzypp-bindings-0.5.9.new/swig/Capabilities.i
--- libzypp-bindings-0.5.9/swig/Capabilities.i	2011-03-01 20:32:06.000000000 +0800
+++ libzypp-bindings-0.5.9.new/swig/Capabilities.i	2011-09-14 13:17:20.273495831 +0800
@@ -8,3 +8,17 @@
 #ifdef SWIGPERL5
 forwarditer(zypp::Capabilities, zypp::Capability);
 #endif
+
+#ifdef SWIGPYTHON
+%extend  zypp::Capabilities {
+    std::string CapNames()
+    {
+        std::string caps;
+        for (zypp::Capabilities::const_iterator it = self->begin(); it != self->end(); ++it) {
+            std::string tmp = ":" + (*it).asString();
+            caps += tmp;
+        }
+        return caps;
+    }
+}
+#endif
